/** @format */

//As the name suggests queues are like lines the first person who was in the line moves first
let q = [];

//adds the elements in the end of the array
q.push(0);
q.push(1);
q.push(2);

console.log(q);

//removes the elements from the start of the array
q.shift();

// Another way is by using pop and unshift
console.log(q);
